export function outputProjectsHandler(projects){
    let projectsBody = document.getElementById('projects-table');
    let projectTemplate = projectsBody.querySelector('project-info').cloneNode(true);
    // !!!!!!!!!!!!
    for(let i = 0; i < projects.length; i++){
        
    }
}